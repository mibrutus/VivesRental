<UserControl x:Class="VivesRental.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:VivesRental.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance {x:Type viewModels:HomeViewModel},IsDesignTimeCreatable=False}">
	<Grid>
        <StackPanel>
	        <TextBlock>Home View</TextBlock>  
            <Button Content="Navigate to new Verhuur Informatie" Command="{Binding NavigateToVerhuurInformatieCommand}"></Button>
        </StackPanel>
		 
    </Grid>
</UserControl>
